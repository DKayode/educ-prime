# Environment files
.env
.env.local
.env.production
# Catch dev variants like backend/my.env / staging.env so they can't
# leak the way `backend/my.env` did once.
*.env
!.env.example

#Docker Image Orchestration
docker-compose.dev.yml

# Docker volumes
postgres_data/
postgres_data_dev/

# Certbot runtime state (ACME account keys, live/archived certs) — never commit
certbot/conf/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies (if installed at root)
node_modules/

# Build outputs
dist/
build/

# Misc
.DS_Store
Thumbs.db
*.backup
*.tmp

backend/config/config.json

CLAUDE.md 
AGENTS.md
